# Reference only — the actual config is generated at container startup by entrypoint.sh
# from environment variables. See docker-compose.yml for env var configuration.
#
# [server]        port = 1933, data_dir = /data
# [embedding]     provider=openai, api_base=OPENROUTER, model=EMBEDDING_MODEL, mode=dense
# [llm]           provider=openai, api_base=OPENROUTER, model=OPENVIKING_LLM_MODEL
